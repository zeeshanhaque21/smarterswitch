name: smarterswitch
description: "Phone-migration app that merges with deduplication."
publish_to: 'none'
version: 0.5.0+8

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  crypto: ^3.0.5
  go_router: ^14.6.2
  flutter_riverpod: ^2.6.1
  xml: ^6.5.0
  args: ^2.5.0
  permission_handler: ^11.3.1
  cryptography: ^2.7.0
  nsd: ^5.0.1
  network_info_plus: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
